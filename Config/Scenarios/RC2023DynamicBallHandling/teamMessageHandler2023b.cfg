totalMessageBudget = 1200;
maxSendInterval = 1000;
minSendInterval = 1000; // this is the minimum send interval (assuming there is a reason to send)
networkTimeout = 10000;
minTimeBetween2RejectSounds = 5000;
sendMirroredRobotPose = false;
minDistanceLastTeamBall = 1000;
returnFromPenaltyDelay = 3000; // should be similar to lookaround delay in BasicReturnFromPenalizedTask
maxTimeWithoutMessageBeforePlaying = 3000; // send a message on start of playing if no message in this time before the transition 