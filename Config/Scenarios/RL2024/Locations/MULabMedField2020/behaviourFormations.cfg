// a set of possible formations to used in world (field) coordinates.
// Values should be considered in light of the active field dimensions.
// All positions should be understood as field coordinates unless otherwise
// specified

// The defaults here are a for the MU medium size 2020 field

readyStateDefaultKickoffOurs = [ // poses listed by player number
  {
    poses = [ // ensure these are appropriate to your field dimensions
      { rotation = 0deg; translation = { x = -3225; y = 0; }; },    // 1 - goal line
      { rotation = 0deg; translation = { x = -2400; y = -275; }; }, // 2 - defender - outside goal
      { rotation = 0deg; translation = { x = -2000; y = 275; }; },  // 3 - defender - outside goal
      { rotation = 0deg; translation = { x = -1400; y = -900; }; }, // 4 - rear mid
      { rotation = 0deg; translation = { x = -200; y = 0; }; }      // 5 - striker
    ];
  }
];
readyStateDefaultKickoffTheirs = [ // poses listed by player number
  {
    poses = [ // ensure these are appropriate to your field dimensions
      { rotation = 0deg; translation = { x = -3225; y = 0; }; },     // 1
      { rotation = 0deg; translation = { x = -2400; y = -220; }; },  // 2
      { rotation = 0deg; translation = { x = -2400; y = 300; }; },   // 3
      { rotation = 0deg; translation = { x = -1250; y = -1100; }; }, // 4
      { rotation = 0deg; translation = { x = -900; y = 100; }; },   //5
    ];
  } 
];
normalTacticFormation = [
  {
    defenderConstraints = { // specified for left side, can mirror if needed
      ballXRange = { min = -2500; max = 1000; }; // outside goal area up to opponent half
      ballYRange = { min = 300; max = 2250; }; // left side of field
      robotXRange = { min = -2800; max = -1600; }; // inside goal area up to midway in own half
      robotYRange = { min = 200; max = 1100; }; // scaled to be on intercept from 1000,3000 to -4500,300
    };
    winger1Constraints = { // specified for left side, can mirror if needed
      ballXRange = { min = -1600; max = 1000; }; // midway thru half up to opponent half
      ballYRange = { min = -1600; max = 1600; }; // full field width less some margin
      robotXRange = { min = -2300; max = -1400; }; // just inside penalty area to a bit more than midway forward in own half
      robotYRange = { min = 200; max = 1200; }; // left side of goal area
    };
    winger2Constraints = { // specified for left side, can mirror if needed
      ballXRange = { min = -1600; max = 1000; }; // midway thru half up to opponent half
      ballYRange = { min = -1600; max = 1600; }; // full field width less some margin
      robotXRange = { min = -1600; max = -700; }; // 700mm forward of winger1
      robotYRange = { min = 200; max = 1200; }; // left side of goal area
    };
  } 
];
