switchToPathPlannerDistance = 1000; // If the target is further away than this distance, the path planner is used.
switchToLibWalkDistance = 900; // If the target is closer than this distance, LibWalk is used.
goalkeeperIgnoreObstaclesDistanceStop = 600; // If the target is further away than this distance, the goalkeeper stop ignoring obstacles.
goalkeeperIgnoreObstaclesDistanceStart = 500; // If the target is closer than this distance, the goalkeeper will start ignoring obstacles
forwardFastKickRange = { min=1000; max=9000; }; // the min and max distance of forwardFastLeft/Right kicks - RV:rough estimate based on kick length calibration test for RC2022
